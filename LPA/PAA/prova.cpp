#include <iostream>
#include <algorithm>
#include <vector>
using namespace std;

int main()
{

    var intervalos[];
    var subconjunto[];

    ordena(intervalos);
    subconjunto.insere(intervalos[0])

        for (int i = 0; i < intervalos.size; i++)
    {

        if (intervalos[i].f == intervalos[i + 1].s)
        {
            if (!subconjunto.contains(intervalo[i]))
                subconjunto.insere(intervalos[i + 1])
                    subconjunto.insere(intervalos[i + 1])
        }
    }
}